---
title: "Graphiques communs"
author: "Groupe OPEN 2022"
date: "`r format(Sys.time(), '%d %B %Y')`"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r warning, include=FALSE}
knitr::opts_chunk$set(warning = FALSE, message = FALSE) 
```

```{r import, include=FALSE}
library(tidyverse)
library(nycflights13)

```

Voici les différents graphiques que le groupe OPEN22 a produit :

# Graphique CB :

Nous observons une augmentation linéaire du temps de vol en fonction de la distance du trajet. On peut voir également une variabilité de temps au sein de chaque trajet.

```{r Graphique_CB, echo = FALSE, eval = TRUE}
source("source/Graphique_CB.R")
```

# Graphique LD :
Le jeu de données mtcars compare plusieurs modèles de véhicules.
Vous trouverez ci-dessous un graphique représentant la consommation (mpg) en fonction de la puissance (cv), le temps d’accélération (qsec) est également mentionné.

```{r Graphique_LD, echo = FALSE, eval = TRUE} 
# changer FALSE en TRUE !!
source("source/Graphique_LD.R")
```

# Graphique JH :

```{r Graphique_JH, echo = FALSE, eval = TRUE}
source("source/Graphique_JH.R")
```

Ce diagramme de densité permet d'observer la distribution d'une variable continue ici, la taille des personnages.

# Graphique GK :

```{r Graphique_GK, echo = FALSE, eval = TRUE}
source("source/Graphique_GK.R")
```
À Partir de 2015, beaucoup plus d'espèces ont été observées.
les espèces de papillons présentes au début des observations (*Limenitis camilla*, *Lycaena tityrus*, *Lycaena phlaeas*) ont disparu tandis que d'autres ont apparu.

# Graphique ML

Il est possible d'observer un histogramme présentant la longueur des sépales d'iris. 

```{r Graphique_ML, echo = FALSE, eval = TRUE}
source("source/Graphique_ML.R")
```

# Graphique CL :

```{r Graphique_CL, echo = FALSE, eval = TRUE}
source("source/Graphique_CL.R")
```

# Graphique PP :
Le jeu de données nycflights13 regroupe des informations issues de différentes compagnies aériennes qui volent à partir de différents aéroports de New York City. Cela concerne notamment des détails sur les vols, les avions et la météo au cours de l'année 2013.
En utilisant ce jeu de données, nous avons établi un graphique des proportions de vols réalisés par chacune des compagnies aériennes (carrier). 

```{r Graphique_PP, echo = FALSE, eval = TRUE}
source("source/Graphique_PP.R")
```

Ainsi, en 2013, la compagnie aérienne ayant réalisé le plus grand nombre de vols au départ des différents aéroports de New York est United Airlines (UA). Elle est suivie par JetBlue (B6), ExpressJet (EV) et Delta (DL).

# Graphique LS :

```{r Graphique_LS, echo = FALSE, eval = FALSE}
source("source/Graphique_LS.R")
```

# Graphique HV :

Ce graphique permet de voir la répartition de la taille en fonction du genre grâce à des boites à moustache. 

```{r Graphique_HV, echo = FALSE, eval = TRUE}
source("source/Graphique_HV.R")
```



 




 
 
